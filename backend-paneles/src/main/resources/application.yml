server:
  port: 8081

app:
  auth:
    allow-test-user-header: false

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://db.mvtdklxcdxfgrwqhbdwz.supabase.co:5432/postgres}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:supabase123$}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
  sql:
    init:
      mode: always

rmi:
  registry-port: 1100
  auth-host: localhost
  auth-port: 1099
